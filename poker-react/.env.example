# Supabase (B1 Auth) — copia questo file in `.env` e riempi coi valori del TUO progetto
# (Supabase Dashboard -> Project Settings -> API). Il `.env` reale è gitignorato.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-public-key>
