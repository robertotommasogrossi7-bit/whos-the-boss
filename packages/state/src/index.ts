/* @whos-the-boss/state — cervello applicativo condiviso (store Zustand + selettori),
   con STORAGE e AUTH iniettati per piattaforma. La web/mobile creano il loro store
   con createAppStore({ storage, auth }). */
export * from './store';
