# Dipendenze (per il futuro progetto React)
node_modules/
dist/
build/
.vite/

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Log
*.log
npm-debug.log*

# Env
.env
.env.local
.env.*.local

# Cartelle utente (non versionarle)
Altro/
Partite_giocate/

# Config locale Claude Code
.claude/

# ── Documenti di processo / memoria AI (restano LOCALI) ──
# Su GitHub deve apparire solo l'app + README + LICENSE.
# Tutta la memoria del progetto (CONTESTO, DECISIONI, IDEE, *_SPEC, *_PROMPT,
# *_MAP) vive nella cartella _processo/ (con _processo/archivio/ per gli spenti).
/_processo/
# Reti di sicurezza: ignora anche eventuali .md sparsi nella root (tranne README).
/*.md
!/README.md
