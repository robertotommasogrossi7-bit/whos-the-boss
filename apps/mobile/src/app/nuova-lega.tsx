import Placeholder from '@/components/Placeholder';

/* Creazione nuova lega (form) — in arrivo. Per ora placeholder. */
export default function NuovaLega() {
  return <Placeholder title="Nuova lega" hint="Form di creazione lega · in arrivo (R1.4)" />;
}
